services:
  - type: web
    name: o-and-m-online
    runtime: go
    buildCommand: cd server && go build -o server .
    startCommand: cd server && ./server
    envVars:
      - key: PORT
        value: 10000